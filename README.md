# Sparkle Cleaner

Open-source Mac cache cleaner. Free scan, pay $1 to execute. Every command shown before it runs.

## Quick Start
```bash
curl -fsSL https://sparkle-cleaner.vercel.app/install.sh | bash
sparkle scan
```